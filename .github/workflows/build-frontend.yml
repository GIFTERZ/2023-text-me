name: Build Frontend Image

on:
  pull_request:
    branches:
      - production
      - master
    paths:
      - "frontend/**"
  workflow_dispatch:

default:
  run:
    working-directory: frontend

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js 
